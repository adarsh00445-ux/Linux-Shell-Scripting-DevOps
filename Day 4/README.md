## Day 4 — File Backup Script

This script creates a backup of a file using command-line arguments. It takes a filename as an argument ($1), checks if it exists, and creates a copy with a .bak extension. If the file is missing, it exits with an error status 1.

Run: `./backup.sh filename.txt`
