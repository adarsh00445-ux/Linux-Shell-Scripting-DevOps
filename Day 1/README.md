## Day 1 — Find Errors in Log Files

This script looks inside a log file named server.log. It finds every line that has the word "error" in it, even if it is uppercase or lowercase. It prints out the matching lines along with their line numbers so you can find them easily.

Run: `./log_monitor.sh`