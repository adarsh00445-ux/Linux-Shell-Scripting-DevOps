\# Day 8 — Count Errors in Log Files



This script looks inside a file passed as an argument. It validates if the file exists and counts how many lines contain the word "error" in it. Finally, it prints out the total count.



Run: ./error\_counter.sh 

