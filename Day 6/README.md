## Day 6 — Interactive File Existence Checker

This script asks the user to type in a filename using the read command. It then checks if that specific file exists on the system. If the file is found, it prints "File found". If the file is missing, it prints "File not found" and stops the script immediately with exit code 1.

Run: `./scripting_practice.sh`
